from random import random
# HW-SPLIT IN HALF
# Result = ‘aaaaabbbbbc’
# string_s = 'bbbbbcaaaaa'
# if len(string_s)%2 == 0:
#     s1 = string_s[:len(string_s) // 2]
#     s2 = string_s[len(string_s) // 2:]
#     print(s2 + s1)
# if len(string_s) % 2 != 0:
#     s1 = string_s[:len(string_s) // 2 + 1]
#     s2 = string_s[len(string_s) // 2 + 1:]
#     print(s2 + s1)
# function
# def spli

# HW2-Unique Characters in String
# string_d = 'apple'
# new_set = set(string_d)
# Missing element
# zip func compares to arrays and and matches the same numbers
#
# Move all 0s to the end
# def move_zeros(zeros):
#     for x in zeros:
#         if x == 0:
#             zeros = zeros.append(x)
#     return x
# test_data = [0, 1, 3, 4, 0, 9]
# print(move_zeros(test_data))









